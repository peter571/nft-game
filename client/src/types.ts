export interface characterDataProp {
  name: string;
  imageURI: string;
  hp: number;
  maxHp: number;
  attackDamage: number;
}

export interface ProviderProp {
    children: React.ReactNode;
  }
  